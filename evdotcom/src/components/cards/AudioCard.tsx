interface CardProps {
  title: string;
  subtitle: string;
  stack: string;
  image: any[];
  link: string;;
}

export const AudioCard = ({ title, subtitle, stack, image, link }: CardProps) => {
  return <>AudioCard</>;
};
